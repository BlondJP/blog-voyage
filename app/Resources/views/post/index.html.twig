{% extends "base.html.twig" %}

{% block extendedBody %}

  {% for post in posts %}

          <h2>{{ post.title|e }}</h2>
          <p>{{ post.content|e }}</p>
          <br>

  {% endfor %}

  <a class="btn btn-lg btn-primary" href="/posts/{{precedent}}" role="button"> &laquo; Precedent</a>
  <a class="btn btn-lg btn-primary"  role="button">{{page}}</a>
  <a class="btn btn-lg btn-primary" href="/posts/{{suivant}}" role="button">Suivant &raquo;</a>

{% endblock %}
